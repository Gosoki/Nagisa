你现在是一个资深的 Web 产品架构师、3D 互动网站设计师、实时多人系统工程师和前端技术负责人。请直接参考这个网站：https://messenger.abeto.co/ 请严格按它的**整体风格、资源组织、架构思路、操作逻辑、交互节奏、气质、多人体验方式**来做。 **不要自行加戏，不要额外发挥，不要美化改造成另一个产品。** 在尽量仿照该网站的前提下，只替换我明确提出的内容要求。没有说明就参考原网站来实现。。 --- # 核心要求 1. 这是一个**网站应用**。 2. 必须支持**多人在线**。 3. 必须支持**多活动同步**。 4. 必须支持**实时同步**。 5. 除了地图 / 玩家活动场景以外，其余尽量仿照参考网站。 6. 地图必须改成**日式小岛**，不是球形，不是星球，不是宇宙风。 7. **UI 要重新设计**，但仍然要符合参考网站整体气质。 8. 允许无人值守推进。 9. 可以自由使用服务器资源，自由开 agent 和专家，自由调研、验证、重构、测试、优化。 10. 必须把代码、注释、文档一起做好。 --- # 参考方式 请把参考网站当作“主模板”，尽量沿用它的： * 整体审美 * 场景密度 * 镜头感 * 材质感 * 风格化程度 * 多人在线感觉 * 交互节奏 * 信息呈现方式 * 页面组织方式 * 资源组织方式 * 系统架构思路 **不要改成别的产品方向。** **不要加入额外的风格化解释。** **不要把它做得比参考网站更花哨。** --- # 我要求你替换的部分 ## 场景地图 把参考网站里的地图/世界，替换为一个原创的**日式小岛**： * 四面环海 * 有码头 * 有灯塔 * 有日式建筑群 * 有海边步道 * 有活动主区 * 有分活动区 * 有公告区 * 有观景区 * 有休息区 * 适合多人在线活动和聚集 要求： * 不做球形地图 * 不做宇宙星球 * 不做赛博城市 * 不做奇怪的重主题包装 * 直接做成参考网站那种“可以进入、可以漫游、可以待着”的世界 ## UI UI 需要重新设计，但必须符合参考网站气质。 要求： * 轻 * 薄 * 克制 * 清晰 * 不抢场景 * 不像企业后台 * 不像传统官网 * 不像重游戏 HUD 可以重新设计按钮和信息层级，但整体要和参考网站保持同一世界观。 ## 多人在线 必须支持： * 多人实时同步位置 * 多人实时同步动作 * 多活动并行 * 活动之间切换 * 房间管理 * 观众 / 参与者模式 * 公告同步 * 签到同步 * 活动开始 / 结束同步 * 管理员 / 主持人控制 * 断线重连 --- # 资源策略 资源处理按这个顺序： 1. 本地能用的资源优先本地用 2. 找不到的自己建 3. 不能直接复用的日式建筑、灯塔、码头、植被、海景等统一自建 4. 风格要和参考网站一致 5. 资源要可复用、可扩展、可维护 --- # 操作逻辑 请尽量保持参考网站的操作逻辑： * 进入后就能进入世界 * 可以自由移动 * 可以看到其他人 * 可以加入活动 * 可以切换活动 * 可以查看公告 * 可以轻量互动 * 可以快速理解当前状态 * 不要做复杂菜单套复杂菜单 * 不要做成后台系统 * 不要把交互做重 * 不要破坏参考网站那种安静克制的感觉 --- # 技术架构 请直接给出可落地方案，包括： ## 前端 * 渲染方案 * 场景管理 * 角色控制 * 资源加载 * UI 系统 * 动作系统 * 房间切换 * 状态同步 * 性能优化 * 移动端适配 ## 后端 * 用户与会话管理 * 房间管理 * 多活动管理 * 广播系统 * 状态同步 * 权限系统 * 活动调度 * 数据存储 * 日志监控 * 容错与恢复 ## 数据结构 请设计关键实体与字段： * 用户 * 活动 * 房间 * 场景 * 角色状态 * 公告 * 签到记录 * 互动事件 * 资源索引 * 管理员操作日志 ## 通信协议 请说明： * 连接建立流程 * 心跳 * 事件同步格式 * 房间加入 / 离开 * 状态快照 * 增量更新 * 断线重连 * 多活动广播 * 服务端权威逻辑 ## 性能策略 请覆盖： * 按需加载 * 分区加载 * 视距裁剪 * 人数上限策略 * 状态合并 * 资源缓存 * 场景切换优化 * 低端设备兼容 * 移动端内存控制 --- # 代码质量与交付 这次必须把代码、注释、文档一起做好。 你必须： * 为关键模块写清晰注释 * 为核心函数、类、组件补充说明 * 为复杂流程写设计说明 * 为项目补充完整文档 * 文档和代码必须一致 * 结构要清晰，后续接手的人能直接看懂 文档至少包括： * 项目概述 * 技术栈说明 * 启动与部署说明 * 目录结构说明 * 运行时配置说明 * 实时同步协议说明 * 多活动同步说明 * 房间与权限说明 * 资源管理说明 * 后续扩展建议 --- # 无人值守要求 这次任务是无人值守推进，请尽可能自主完成。 允许你： * 使用服务器资源 * 拆分任务 * 创建多个 agent / 专家协作 * 调研、验证、重构、测试、优化 * 自主决定实现路径 要求： * 主动把任务做完整 * 不要只给概念 * 不要只做半成品 * 不要省略文档 * 不要省略注释 * 不要把复杂部分留到以后 --- # 输出要求 请最终输出： 1. 产品整体方案 2. 视觉与场景方案 3. 多人在线与同步机制 4. 多活动同步方案 5. 实时通信方案 6. 前后端架构 7. 数据结构设计 8. 核心页面 / 核心组件 9. 资源策略 10. MVP 范围 11. 后续迭代路线 12. 推荐技术栈 13. 目录结构建议 14. 关键伪代码或接口设计 15. 代码注释与文档组织方案 16. 部署与运行说明 --- # 额外约束 * 地图必须是日式小岛，不要球形 * UI 必须重新设计 * 其余尽量对齐参考网站 * 不要做成传统网页后台 * 不要做成普通 2D 官网 * 不要做成只有聊天的产品 * 不要让多人同步影响安静感 * 不要让场景过空 * 不要让 UI 压过场景 * 必须是可上线、可扩展、可运营的 Web 应用 请直接开始输出完整方案，按“参考网站对齐 → 差异点实现 → 架构 → 数据 → 实现 → 文档 → 部署”的顺序来写。